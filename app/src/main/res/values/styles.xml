<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/myColorPrimary</item>
        <item name="colorPrimaryDark">@color/myColorPrimaryDark</item>
        <item name="colorAccent">@color/myColorAccent</item>
    </style>

    <!-- TOOLBAR THEME -->
    <style name="ToolbarTheme" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
        <!-- Color of the title text in the Toolbar -->
        <item name="android:textColorPrimary">@color/colorAccent</item>
        <item name="android:actionOverflowButtonStyle">@style/CustomDots</item>
        <item name="drawerArrowStyle">@style/CustomBurger</item>
        <item name="colorControlNormal">@android:color/white</item>
    </style>

    <style name="Toolbar.Popup" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:colorBackground">@android:color/white</item>
        <item name="android:textColor">@android:color/black</item>
    </style>

    <!-- TOOLBAR 3 DOTS THEME -->
    <style name="CustomDots" parent="@android:style/TextAppearance.Widget.IconMenu.Item">>
        <item name="android:src">@drawable/more_vert_white_24</item>
    </style>

    <!-- TOOLBAR BURGER THEME -->
    <style name="CustomBurger" parent="Base.Widget.AppCompat.DrawerArrowToggle">>
        <item name="color">@android:color/white</item>
    </style>

    <!-- TOOLBAR TITLE THEME -->
    <style name="Toolbar.TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">21sp</item>
        <item name="android:textStyle">italic</item>
    </style>

</resources>
